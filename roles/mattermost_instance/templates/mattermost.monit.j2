check host {{ mattermost_domain }} with address {{ mattermost_domain }}
   start program = "/bin/systemctl start {{ mattermost_instance_service }}"
   stop  program = "/bin/systemctl stop {{ mattermost_instance_service }}"

# HTTPS check
    if failed
          port 443 type tcpSSL protocol http
       for 1 times within 1 cycles
       then restart
